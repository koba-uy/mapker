{
  "name": "mapker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm run build-api && npm run build-app",
    "build-api": "npm install --prefix api/ && npm run build --prefix api/ ",
    "build-app": "npm install --prefix app/ && npm run build --prefix app/",
    "heroku-postbuild": "npm run build",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Agustín Rodríguez",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.17.2",
    "cookie-parser": "^1.4.3",
    "express": "^4.15.4"
  }
}
