{
  "name": "mapker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm install --prefix api/ && npm install --prefix app/ && npm run build --prefix app/ && node index.js",
    "start-dev": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Agustín Rodríguez",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.4"
  }
}
